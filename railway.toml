[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build --workspace=packages/frontend && npm run build --workspace=packages/backend"

[deploy]
startCommand = "cd packages/backend && npx prisma db push && npx prisma db seed && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
